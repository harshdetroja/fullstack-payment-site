export const H1 = ({ title }) => {
  return <h1 className="text-black text-3xl font-bold text-center">{title}</h1>;
};

export const SubH1 = ({ title }) => {
  return <p className="text-center  text-zinc-500">{title}</p>;
};
